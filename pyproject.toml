[project]
name = "prestige-settlement-automation"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "playwright",
    "python-dotenv",
    "openpyxl",
]

[tool.setuptools.packages.find]
where = ["src"]

dependencies = [
    "playwright",
    "python-dotenv",
]